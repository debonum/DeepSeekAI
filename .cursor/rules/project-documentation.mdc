---
description: 
globs: 
alwaysApply: true
---
# DeepSeek AI 浏览器扩展项目文档

## 项目整体架构概览

DeepSeek AI 是一个基于 Chrome/Edge 扩展的 AI 助手工具，采用 Manifest V3 规范开发。项目使用 Webpack 作为构建工具，支持多语言（i18n）和主题切换。

### 技术栈
- 构建工具：Webpack 5
- 包管理器：pnpm
- 核心依赖：
  - markdown-it：Markdown 渲染
  - highlight.js：代码高亮
  - mathjax：数学公式渲染
  - interactjs：拖拽交互
  - openai：AI API 调用

## 代码文件依赖关系

```
src/
├── manifest.json          # 扩展配置文件
├── background.js         # 后台服务工作进程
├── content/             # 内容脚本目录
├── popup/               # 弹出窗口目录
├── icons/               # 图标资源
├── Instructions/        # 使用说明
└── _locales/           # 国际化资源

```

### 核心模块职责

1. **Background Service (background.js)**
   - 处理 API 请求代理
   - 管理扩展状态
   - 处理跨域请求
   - 管理上下文菜单

2. **Content Scripts (content/)**
   - 注入页面 UI
   - 处理用户交互
   - 与后台服务通信

3. **Popup (popup/)**
   - 提供设置界面
   - 管理用户配置
   - 展示使用说明

4. **国际化 (_locales/)**
   - 支持多语言界面
   - 管理翻译资源

## 权限与安全

### 扩展权限
```json
{
  "permissions": [
    "storage",        // 存储访问
    "contextMenus",   // 右键菜单
    "scripting",      // 脚本注入
    "commands",       // 快捷键
    "tabs"           // 标签页访问
  ],
  "host_permissions": [
    "<all_urls>"     // 跨域请求
  ]
}
```

### 安全策略
- CSP (Content Security Policy) 限制
- 沙箱环境隔离
- 资源访问控制

## 构建与部署

### 构建命令
```bash
npm run build        # 开发构建
npm run build:zip    # 打包为 zip
npm run build:chrome # Chrome 发布包
npm run build:edge   # Edge 发布包
npm run build:all    # 构建所有版本
```

### 构建配置
- Webpack 5 配置
- 资源压缩与优化
- 开发热重载支持

## 待补充内容
1. 功能模块调用逻辑
2. 关键代码文件定位索引
3. API 接口文档
4. 组件系统文档
5. 事件系统文档

## Content 目录详细说明

### 目录结构
```
content/
├── content.js           # 内容脚本入口
├── popup.js            # 弹出窗口逻辑
├── components/         # UI 组件
│   ├── IconManager.js      # AI 图标管理
│   ├── QuickActionButtons.js # 快速操作按钮
│   ├── InputContainer.js    # 输入容器
│   ├── ResponseContainer.js # 响应容器
│   └── DragHandle.js       # 拖拽句柄
├── services/          # 服务层
│   └── apiService.js      # API 服务
├── styles/           # 样式文件
└── utils/           # 工具类
    ├── constants.js       # 常量定义
    ├── themeManager.js    # 主题管理
    ├── popupStateManager.js # 弹出窗口状态管理
    ├── markdownRenderer.js # Markdown 渲染
    └── scrollManager.js    # 滚动管理
```

### 核心功能模块

#### 1. 内容脚本入口 (content.js)
- 初始化 UI 组件
- 处理文本选择事件
- 管理弹出窗口生命周期
- 处理用户交互事件
- 与后台服务通信

#### 2. UI 组件系统
- **IconManager**: 管理 AI 图标显示和交互
- **QuickActionButtons**: 提供快速操作按钮
- **InputContainer**: 处理用户输入
- **ResponseContainer**: 展示 AI 响应
- **DragHandle**: 处理窗口拖拽

#### 3. 服务层
- **apiService**: 处理与 AI 服务的通信
  - 请求构建
  - 响应处理
  - 错误处理
  - 流式响应支持

#### 4. 工具类
- **constants**: 定义全局常量
- **themeManager**: 管理主题切换
- **popupStateManager**: 管理弹出窗口状态
- **markdownRenderer**: 处理 Markdown 渲染
- **scrollManager**: 管理滚动行为

### 主要功能流程

1. **文本选择流程**
   ```mermaid
   graph TD
   A[用户选择文本] --> B[触发选择事件]
   B --> C[显示 AI 图标]
   C --> D[用户点击图标]
   D --> E[创建弹出窗口]
   E --> F[显示快速操作按钮]
   ```

2. **AI 交互流程**
   ```mermaid
   graph TD
   A[用户输入/选择文本] --> B[构建请求]
   B --> C[发送到后台服务]
   C --> D[接收流式响应]
   D --> E[渲染 Markdown]
   E --> F[更新 UI]
   ```

3. **窗口管理流程**
   ```mermaid
   graph TD
   A[创建弹出窗口] --> B[初始化组件]
   B --> C[设置拖拽]
   C --> D[管理状态]
   D --> E[处理关闭]
   ```

### 状态管理
- 使用 Chrome Storage API 管理设置
- 使用 popupStateManager 管理窗口状态
- 使用主题管理器处理主题切换

### 事件系统
- 文本选择事件
- 图标点击事件
- 快速操作事件
- 窗口拖拽事件
- 设置变更事件

## Popup 目录详细说明

### 目录结构
```
popup/
├── popup.html              # 弹出窗口HTML
├── popup.js                # 弹出窗口主入口
├── i18n.js                 # 国际化管理
├── apiKeyManager.js        # API密钥管理
├── EventManager.js         # 事件管理
├── ModelManager.js         # 模型管理
├── ProviderManager.js      # 服务提供商管理
├── ProviderUIManager.js    # 服务提供商UI管理
├── storageManager.js       # 存储管理
├── uiManager.js            # UI管理
└── components/            # UI组件
```

### 核心功能模块

#### 1. 管理器模式架构
项目的弹出窗口采用了管理器模式架构，主要由以下管理器组成：

- **PopupManager**: 中央控制器，协调各管理器
- **I18nManager**: 处理国际化和多语言支持
- **UiManager**: 管理UI元素和交互
- **StorageManager**: 负责配置的存储和读取
- **ProviderManager**: 管理AI服务提供商
- **ModelManager**: 管理模型列表和选择
- **ProviderUIManager**: 管理服务提供商的UI表示
- **ApiKeyManager**: 管理API密钥
- **EventManager**: 处理事件监听

#### 2. ProviderManager
```mermaid
classDiagram
    class ProviderManager {
        +defaultProviders[]
        +customProviders[]
        +hiddenProviders[]
        +getAllVisibleProviders()
        +saveCustomProvider()
        +hideDefaultProvider()
        +getProviderById()
        +getApiKey()
        +validateApiKey()
        +getModels()
    }
```

**功能职责**：
- 管理默认和自定义服务提供商
- 提供API密钥管理
- 提供模型管理
- 验证API密钥和连接

#### 3. ModelManager
```mermaid
classDiagram
    class ModelManager {
        +updateModelOptions()
        +createCustomModelDropdown()
        +showAddModelDialog()
        +showDeleteModelDialog()
        +handleDeleteModel()
        +handleSaveModel()
    }
```

**功能职责**：
- 更新模型下拉选项
- 创建自定义模型下拉菜单
- 处理模型的添加和删除
- 管理模型UI交互

#### 4. StorageManager
```mermaid
classDiagram
    class StorageManager {
        +getSettings()
        +saveSettings()
        +saveApiKey()
        +getApiKey()
        +saveModel()
        +getModel()
        +saveProvider()
        +getProvider()
    }
```

**功能职责**：
- 管理用户设置的存储
- 处理API密钥的安全存储
- 提供统一的存储访问接口

### 主要功能流程

1. **设置初始化流程**
   ```mermaid
   graph TD
   A[页面加载] --> B[创建所有管理器]
   B --> C[I18nManager更新标签]
   C --> D[初始化事件监听器]
   D --> E[加载初始状态]
   E --> F[更新UI元素]
   ```

2. **服务提供商切换流程**
   ```mermaid
   graph TD
   A[选择服务提供商] --> B[触发provider-change事件]
   B --> C[更新UI]
   C --> D[更新API密钥显示]
   D --> E[更新模型下拉菜单]
   E --> F[保存设置]
   ```

3. **API密钥验证流程**
   ```mermaid
   graph TD
   A[输入API密钥] --> B[点击验证按钮]
   B --> C[调用validateApiKey]
   C --> D[发送测试请求]
   D --> E1[成功]
   D --> E2[失败]
   E1 --> F1[显示成功提示]
   E2 --> F2[显示错误提示]
   ```

### 服务提供商系统

Popup模块支持多种AI服务提供商：

1. **默认服务提供商**：
   - DeepSeek
   - OpenRouter
   - AiHubMix
   - SiliconFlow
   - VolcEngine
   - TencentCloud
   - IFlytekStar
   - BaiduCloud
   - Aliyun

2. **自定义服务提供商**：
   - 用户可以添加自定义的服务提供商
   - 支持自定义API端点
   - 支持自定义模型列表

### 事件管理系统

EventManager负责管理所有UI事件：

- 服务提供商切换事件
- 模型选择事件
- API密钥输入事件
- 设置切换事件
- 语言切换事件
- 验证按钮点击事件
- 添加/删除模型事件

## 背景服务分析 (background.js)

### 功能概述

Background Service是扩展的核心后台进程，主要负责：
1. 处理跨域API请求代理
2. 管理请求的中断和取消
3. 处理上下文菜单交互
4. 提供存储访问接口

### 消息处理系统

```mermaid
flowchart TD
    A[内容脚本] -->|发送消息| B{Background}
    B -->|getSettings| C[获取设置]
    B -->|proxyRequest| D[代理API请求]
    B -->|abortRequest| E[中断请求]
    B -->|openPopup| F[打开设置页]
    D -->|streamResponse| A
```

#### 1. 设置获取 (getSettings)
- 从Chrome存储中读取API密钥、自定义URL等
- 处理自定义服务提供商的设置
- 返回完整的设置对象

#### 2. 请求代理 (proxyRequest)
- 接收来自内容脚本的API请求
- 添加适当的API密钥和头信息
- 处理流式响应
- 通过消息通道返回数据

#### 3. 请求中断 (abortRequest)
- 使用AbortController取消正在进行的请求
- 管理请求控制器映射
- 清理与取消相关的资源

### 上下文菜单系统

- 创建右键菜单选项
- 处理菜单点击事件
- 将选中的文本发送给内容脚本

### 关键实现技术

1. **流式响应处理**：
   - 使用ReadableStream API处理流式响应
   - 逐行解析流数据
   - 将数据块通过消息通道发送给内容脚本

2. **请求管理**：
   - 使用AbortController管理请求生命周期
   - 通过tabId映射请求控制器
   - 提供取消请求机制

3. **错误处理**：
   - 全面的请求错误捕获和处理
   - 详细的错误日志记录
   - 向内容脚本报告错误

## 功能模块调用逻辑

### 用户交互流程

#### 1. 选择文本触发助手流程
```mermaid
sequenceDiagram
    participant 用户
    participant 网页
    participant 内容脚本
    participant 背景服务
    participant AI服务
    
    用户->>网页: 选择文本
    网页->>内容脚本: 触发selectionchange事件
    内容脚本->>网页: 显示AI图标
    用户->>网页: 点击AI图标
    内容脚本->>网页: 创建对话窗口
    用户->>网页: 发送消息
    内容脚本->>背景服务: 请求API调用
    背景服务->>AI服务: 发送请求
    AI服务-->>背景服务: 流式返回响应
    背景服务-->>内容脚本: 转发响应
    内容脚本-->>网页: 渲染响应
```

#### 2. 设置配置流程
```mermaid
sequenceDiagram
    participant 用户
    participant popup界面
    participant 背景服务
    participant 存储API
    
    用户->>popup界面: 点击扩展图标
    popup界面->>存储API: 获取当前设置
    存储API-->>popup界面: 返回设置
    popup界面->>popup界面: 更新UI
    用户->>popup界面: 修改设置
    popup界面->>存储API: 保存设置
    用户->>popup界面: 验证API密钥
    popup界面->>背景服务: 发送测试请求
    背景服务-->>popup界面: 返回验证结果
```

#### 3. 右键菜单快捷方式
```mermaid
sequenceDiagram
    participant 用户
    participant 网页
    participant 背景服务
    participant 内容脚本
    
    用户->>网页: 选择文本
    用户->>网页: 右键点击
    网页->>背景服务: 触发contextMenu事件
    背景服务->>内容脚本: 发送createPopup消息
    内容脚本->>网页: 创建对话窗口
    内容脚本->>网页: 填充选中的文本
```

### 模块间通信流程

#### 1. 内容脚本与背景服务通信
```mermaid
sequenceDiagram
    participant 内容脚本
    participant 背景服务
    participant AI服务
    
    内容脚本->>背景服务: chrome.runtime.sendMessage({action: "proxyRequest"})
    背景服务->>AI服务: fetch(request.url)
    AI服务-->>背景服务: 流式响应
    背景服务->>内容脚本: chrome.tabs.sendMessage({type: "streamResponse"})
```

#### 2. 存储访问流程
```mermaid
sequenceDiagram
    participant 组件
    participant StorageManager
    participant ChromeStorage
    
    组件->>StorageManager: getSettings()
    StorageManager->>ChromeStorage: chrome.storage.sync.get()
    ChromeStorage-->>StorageManager: 返回设置
    StorageManager-->>组件: 返回处理后的设置
    
    组件->>StorageManager: saveSettings(settings)
    StorageManager->>ChromeStorage: chrome.storage.sync.set()
    ChromeStorage-->>StorageManager: 保存完成
```

#### 3. 主题切换流程
```mermaid
sequenceDiagram
    participant 用户
    participant ThemeManager
    participant 文档
    
    用户->>ThemeManager: 切换主题
    ThemeManager->>文档: 添加/移除暗黑模式类
    ThemeManager->>localStorage: 保存主题设置
```

## 关键代码文件定位索引

### 核心功能实现

| 功能 | 文件路径 | 主要函数/类 |
|------|----------|------------|
| 背景服务 | src/background.js | `chrome.runtime.onMessage.addListener()` |
| 内容脚本入口 | src/content/content.js | `document.addEventListener('selectionchange')` |
| AI图标管理 | src/content/components/IconManager.js | `createIcon()`, `createSvgIcon()` |
| 弹出窗口创建 | src/content/popup.js | `createPopup()` |
| API服务 | src/content/services/apiService.js | `getAIResponse()`, `processStreamData()` |
| Markdown渲染 | src/content/utils/markdownRenderer.js | `render()` |
| 设置菜单入口 | src/popup/popup.js | `PopupManager` |
| 服务提供商管理 | src/popup/ProviderManager.js | `ProviderManager` |
| 模型管理 | src/popup/ModelManager.js | `ModelManager` |
| 存储管理 | src/popup/storageManager.js | `StorageManager` |

### 事件处理

| 事件 | 文件路径 | 处理函数 |
|------|----------|----------|
| 文本选择 | src/content/content.js | `handleSelection()` |
| AI图标点击 | src/content/components/IconManager.js | `handleIconClick()` |
| 弹窗拖拽 | src/content/components/DragHandle.js | `initDrag()` |
| 提交用户输入 | src/content/components/InputContainer.js | `handleSubmit()` |
| 设置项改变 | src/popup/EventManager.js | `initProviderChangeHandler()` |
| 添加模型 | src/popup/ModelManager.js | `showAddModelDialog()` |
| 右键菜单点击 | src/background.js | `chrome.contextMenus.onClicked.addListener()` |

### 国际化与主题

| 功能 | 文件路径 | 主要函数/类 |
|------|----------|------------|
| 多语言支持 | src/popup/i18n.js | `I18nManager`, `updateLabels()` |
| 主题管理 | src/content/utils/themeManager.js | `initTheme()`, `toggleTheme()` |
| 语言资源 | src/_locales/ | 语言JSON文件 |

### API调用与响应处理

| 功能 | 文件路径 | 主要函数/类 |
|------|----------|------------|
| API请求构建 | src/content/services/apiService.js | `buildRequestBody()` |
| API密钥管理 | src/popup/apiKeyManager.js | `ApiKeyManager` |
| 响应流处理 | src/background.js | `fetch().then(response => {...})` |
| 响应渲染 | src/content/components/ResponseContainer.js | `updateContent()` |

### 数据流向图

```mermaid
graph TD
    A[用户输入] --> B[content.js]
    B --> C[apiService.js]
    C --> D[background.js]
    D --> E[AI服务API]
    E --> D
    D --> C
    C --> F[ResponseContainer.js]
    F --> G[markdownRenderer.js]
    G --> H[渲染响应]
    
    I[用户设置] --> J[popup.js]
    J --> K[ProviderManager.js]
    J --> L[ModelManager.js]
    J --> M[StorageManager.js]
    M --> N[Chrome Storage]
    N --> B
```

